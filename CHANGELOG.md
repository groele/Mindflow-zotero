# 📜 MindFlow 版本更新日志 (CHANGELOG.md)

All notable changes to this project will be documented in this file.

## [Zotero v1.6.0] - 2026-09-26

### 生成流程与资料控制
- 生成前可逐项选择摘要、PDF 文字、Zotero 笔记、批注划线和读者评论；未选资料不发送给模型，也不参与引文核对。预览显示所选字符量、实际 PDF 附件及长资料截取情况。
- 资料准备支持取消；旧任务结束时不再改写新任务状态。长 PDF 的等待上限按预计请求次数调整，超时或失败后仍可复用当前会话已完成的分段。
- 深入模式的最终整合压缩分段候选及笔记、批注片段，保留来源编号，并仅在实际送入整合的资料范围内核对引文。导图备注标明整合范围的限制。
- 单条笔记或批注读取失败时继续读取其余条目，并在预览和导图中提示遗漏；增加重新读取 Zotero 资料的入口。

### 验证边界
- 构建、TypeScript 和语法检查通过不等于已完成 Zotero 10 桌面端、真实模型服务、PDF 多附件与跨窗口验收。

## [Zotero v1.5.0] - 2026-09-26

### 可追溯的 AI 论文导图
- 摘要、PDF 文字片段、Zotero 笔记、批注划线和读者评论分别编号；生成结果只在引文与指定编号的文字匹配时确认来源。
- 没有来源编号的引文仅在唯一资料片段匹配时接受；重复、错误编号或不匹配的引文降为待核验，并保留模型原始引文供人工检查。
- 深入模式整合时只接受分段草稿中出现过的 PDF 引文，防止整合阶段新增或改写来源；批注评论只作为读者材料，不会被标为论文原文支持。
- 导图根节点显示证据核对数量；各节点备注标明来源编号、核对状态和局限。PDF 的 P 编号是片段序号，不是页码。

### 验证边界
- 构建流程检查 TypeScript 和宿主脚本语法；真实 Zotero 10 与用户模型服务仍需在桌面端核验。

## [Zotero v1.4.0] - 2026-09-26

### AI 研究导图工作流
- 生成前在本机准备并预览摘要、笔记、批注和 PDF 文字范围；只有再次点击开始分析才发送论文资料。
- 提供快速概览与深入分析：长 PDF 跨区间采样，深入模式分段分析已提取文字并整合；PDF 页数上限可选 50、120 或 200 页。
- 增加阶段进度、当前 HTTP 请求取消、连接测试、明确的服务错误提示及短时间会话缓存。
- 生成结果先保存为可编辑的本机草稿；审阅后明确归档，草稿阶段的 `Ctrl/Cmd+S` 不会绕过审阅。
- 为匹配的 Zotero 批注保留页码标签及定位链接，普通 PDF 文字不生成无依据的逐句页码。

### 数据与验证边界
- 模型请求不携带 Zotero 会话 Cookie，不在 Zotero 调试输出中记录论文请求体；失败请求不会自动重复提交。
- 深入模式可能产生多次模型费用。构建与静态检查不能证明实际 Zotero 10 + 模型服务的完整流程已经运行。

## [Zotero v1.3.0] - 2026-09-25

### AI 论文研究解析
- 在 Zotero 文献右键菜单和 MindFlow 工作台加入单篇论文 AI 解析入口，生成独立、可编辑并可归档的研究导图。
- 按背景、已有问题、目标、研究体系、方法、结果证据、解决的问题、意义、局限和后续验证组织内容。
- 对模型给出的原文短引文执行本地匹配；无可匹配证据的条目标为推断或待核验，资料不足的栏目保留待补充提示。
- 在 Zotero 原生设置面板配置兼容 Chat Completions 的模型地址、名称及本机 API 密钥；只在用户点击生成时发送论文资料。

### 资料边界
- PDF 文字最多读取前 50 页，超长文本截断；扫描件、云端未下载或无法提取文字时回退到摘要、笔记和批注。根节点说明实际资料范围。
- 本版仅完成源码、构建及包内容检查；尚未使用用户模型账户在 Zotero 10 桌面端完成端到端验证。

## [Zotero v1.2.1] - 2026-09-25

### 文档与版本
- 扩充 README：说明 Zotero 入口选择、多份导图、升级备份、跨设备恢复及常见故障排查。
- 同步 Zotero 插件清单、更新链接、工作台版本显示和工作区备份版本号；本版不改变导图功能或数据格式。

### 验证范围
- 本版仅更新文档与版本信息；Zotero 10 图形界面安装和完整交互仍需实机核验。

## [Zotero v1.2.0] - 2026-09-25

### Zotero 原生协同
- 在条目详情侧栏加入 MindFlow 导图区，直接查看、打开和新建该文献的导图；附件变化后刷新列表。
- 侧栏使用 Zotero 官方扩展接口，加载中英文 Fluent 文案，并在插件停用时清理区域、监听器与本地化资源。

### 归档与交互修复
- 分类及多篇文献导图不再自动挂到第一篇文献；手动归档未关联导图时必须明确选中一篇文献。
- 将 .mindflow 附件的双击入口限定在文献条目树，避免误拦截其他区域；对附件中的导图结构作基本检查。
- 文献库只读时在侧栏说明新导图仅能保存在本地。

### 验证范围
- 已构建 Zotero XPI，并由构建流程完成 TypeScript 与宿主脚本语法检查。完整的 Zotero 10 图形界面安装和交互仍需实机核验。

## [Zotero v1.1.0] - 2026-09-25

### 新增
- Zotero 10 原生 MindFlow 设置面板集中管理文献导入、工具栏、编辑默认值、快照和 WebDAV 配置；与工作台共用设置并同步变更。
- 同一文献的多份 .mindflow 附件可从右键菜单分别打开；PDF 批注节点可返回原 PDF 标注位置。
- 导图关联文献后可更新对应子笔记；长摘要、文献笔记和批注保留完整内容。

### 修复与兼容
- 纠正异步 PDF 附件选择、群组库只读反馈、云端附件未下载时的重复新建风险，以及保存状态提前报成功的问题。
- 默认主题、布局、缩放步进与任务优先级接入实际编辑操作；兼容范围明确为 Zotero 10.0.x。
- Zotero 与 Chrome 扩展使用独立版本号；Zotero 发布资产为同版本 XPI。

### 验证范围
- 通过 Zotero XPI 构建（含 TypeScript 与宿主 JavaScript 语法检查）；尚未在 Zotero 10 图形界面完成安装和完整交互验证。

## [3.2.0] - 2026-09-25

### 新增
- 选中节点后可用 Ctrl/Cmd+V 粘贴剪贴板截图，或将单张图片直接拖入目标节点；两者沿用图片压缩、格式限制和存储空间检查。

### 稳定性
- 应用内复制分支会同步更新系统剪贴板的文字和节点标记，粘贴时识别分支与新截图；在文字输入框和弹窗中不拦截粘贴。
- 节点拖拽使用专用数据类型，外部文件拖入不再误触发节点移动；拖拽结束时清理状态。
- 同时发起第二次图片导入时给出明确提示，避免异步处理覆盖先前图片编辑。

## [3.1.1] - 2026-09-25

### 改进
- 在节点悬浮工具栏增加图片图标，直接为当前节点导入或替换图片，并沿用现有的格式、压缩和存储空间检查。

## [3.1.0] - 2026-09-25

### 新增
- 工具栏一键建立图片节点；普通节点也可在属性面板导入、替换或删除图片。
- 图片节点可同时保留可编辑文字，支持纯图片节点以及 80–480 px 显示宽度调整。
- SVG、PNG 和交互 HTML 导出保留图片；JSON 与工作区备份保留可继续编辑的图片数据。

### 稳定性与兼容性
- 导入时校验图片格式、原始文件尺寸和本地剩余空间，并压缩为受限大小的 WebP 数据。
- 备份导入校验节点图片字段，拒绝不受支持的图片内容。
- Markdown、OPML 导出遇到图片时提示其不包含图片，避免误将文字导出当作完整备份。
- 旧导图无需迁移；节点图片为可选字段。扩展本地存储仍受浏览器配额约束，建议定期导出完整工作区备份。

## [3.0.0] - 2026-09-25

### 新增
- 标签聚焦工作台：统计当前导图的标签与匹配主题，定位节点并在画布中突出显示。
- 跨导图主题链接：主题可关联另一篇导图或指定主题，支持单击跳转与失效目标提示。
- 大纲显示分支任务完成数；任务属性可选择优先级。
- 本地与 WebDAV 全量备份恢复前预览新增及覆盖数量。

### 稳定性与数据安全
- 切换导图、新建导图、导入和工作区备份前刷新待保存编辑，失败时阻止继续操作。
- 删除导图经后台顺序写入队列并检查版本；旧窗口保存已删除导图会触发冲突保护。
- 撤销历史写入不再对整棵导图做额外 JSON 序列化，减少大图编辑的主线程开销。
- 导入的外部链接仅允许 HTTPS、HTTP 和 mailto 在节点上打开。
- GitHub Release 作业获得所需的仓库内容写入权限，以便自动附加 ZIP 包。

### 兼容性
- 导图数据新增可选的 `internalLink` 字段，旧文档无需迁移。
- 工作区导入按文档逐篇处理；存储空间耗尽等写入故障仍可能留下部分已导入内容。导入前请保留外部备份。

## [2.6.0] - 2026-09-25

### 新增
- 当前导图任务总览、完成比例，以及状态、逾期和文本筛选。
- WebDAV 独立历史备份文件与指定版本恢复入口。

### 改进
- 多窗口导图写入由扩展后台顺序处理；版本冲突时保留冲突副本。
- 自动云备份在编辑暂停后执行，不阻塞后续本地保存。
- 导入与快照恢复先保存被覆盖文档的恢复点，并检查并发写入。
- WebDAV 权限按目标 HTTPS 主机申请；导入数据与导出样式增加校验。
- 整理历史文档和旧版发布包，更新产品及安全说明。

### 兼容性
- 兼容现有导图数据；旧文档首次保存时补充版本号。
- WebDAV 历史文件会占用服务器空间，目前需手动管理保留数量。

## [2.3.0] - 2026-09-22

### 🎯 可实践人机工效与交互提升 (Practical Usability)
- **画布即时搜索 (`Ctrl + F`)**：支持全图递归全文搜索（标题/备注/标签），悬浮搜索框，结果数量统计，支持 `Enter`/`Shift+Enter` 顺逆向循环导航并自动将镜头平滑飞跃居中目标节点。
- **视口自适应适配**：新增 `Ctrl + 1` 一键计算全图边界并自动最佳缩放适配屏幕居中；`Ctrl + 0` 一键重置为 100% 原始大小。
- **桌面级节点右键菜单 (ContextMenu)**：在节点上右键呼出原生质感菜单（插入子节点、插入同级节点、删除、转为待办任务、复制文本、折叠/展开分支、聚焦子树）。
- **下钻专注模式 (Focus Subtree)**：支持从右键菜单选择聚焦特定子分支，快速理清超大思维导图。
- **优雅导出水印体系**：社区版导出附带精致品牌标识，Pro 版享有纯净无水印 4K PNG 与矢量 SVG 导出。

### 🚀 可部署与应用商店合规 (Deployable & Store Ready)
- **Chrome Web Store 国际化支持 (`_locales`)**：支持中英双语国际化字典，符合 Google Web Store 审核标准。
- **上线与合规法律文件**：新增 `PRIVACY.md`（遵循 GDPR 与 Chrome 单一用途无追踪政策）、`TERMS.md`（服务条款与商用许可）以及 `docs/STORE_LISTING.md`（商店元数据与物料指南）。
- **自动化打包脚本 (`scripts/package-extension.js`)**：一键校验产物并打包为符合 Chrome Web Store 要求的 Zip 归档。
- **GitHub Actions CI/CD 流水线 (`.github/workflows/ci-cd.yml`)**：代码提交及打标签时自动运行测试、构建与打包，并自动发布 Release 资产。

### 💼 可商业化体系 (Commercialization & Pro Architecture)
- **商业授权引擎 (`LicenseService`)**：基于离线非对称校验和算法（`MFPRO-XXXX-XXXX-XXXX-XXXX`），支持 100% 离线验签与持久化。
- **商业路演演示模式 (Presentation Mode)**：支持 `F5` 或工具栏一键进入全屏沉浸式演示，自适应按逻辑层级推进聚焦，胜过 PPT 的演说神器。
- **系统设置新增 Pro 会员特权面板**：清晰展示社区版与 Pro 版功能特权矩阵、序列号激活与注销。
- **单元测试扩充 (12/12)**：新增 License 离线算法验证与全文检索递归测试。

---

## [2.2.0] - 2026-09-22

### ⚙️ 系统设置中心 (Settings Center & Preferences)
- **多级设置直达入口**：在顶部/底部工具栏、左侧微型图标轨、`Ctrl+K` 命令面板以及全局键盘快捷键 `Ctrl+,` 均支持一键弹出设置中心。
- **5 大分类设置面板**：覆盖界面定制、WebDAV 云同步、备份策略、编辑偏好与系统关于。

### 🎛️ 工具栏与界面深度自由定制 (Interface & Toolbar Customization)
- **工具栏位置定制**：支持工具栏置顶（Top，默认）或置底（Bottom），自适应不同窗口高度与阅读习惯。
- **工具栏按钮自由显隐**：支持独立开关 10 项按钮（撤销重做、插入子节点、插入同级节点、删除节点、布局切换、主题切换、导出、收集箱、专注模式、缩放控件），避免小屏/Side Panel 拥挤。
- **画布背景纹理切换**：支持点阵 (Dots)、方格网格 (Grid)、纯净无格 (Blank) 三种纹理实时换肤。

### ☁️ WebDAV 云同步与异地容灾 (WebDAV Cloud Sync)
- **主流网盘一键预设**：内置坚果云（Jianguoyun）、Nextcloud、群晖 NAS 等服务商预设指引。
- **点对点直连无中转**：遵循 Manifest V3 跨域网络规范，密码与密钥仅存放于用户本地 Storage。
- **连通性即时检测**：一键发起 WebDAV `PROPFIND` / `OPTIONS` 检测，反馈详细 HTTP 状态码与中文排错建议。
- **双向数据同步**：支持一键将全量工作区打包为 JSON 远程备份，或从 WebDAV 拉取异地备份安全还原。
- **自动云同步**：支持编辑保存导图时静默同步备份至 WebDAV。

### 🧩 编辑行为与默认偏好 (Default Preferences)
- **默认主题与布局定制**：新建导图时自动应用用户指定的主题与布局。
- **自动展开父分支**：添加子节点时，若父节点处于折叠状态则智能自动展开。
- **任务默认优先级**：可设置新建 Task 时的默认优先级（P1/P2/P3/P4）。

---

## [2.1.0] - 2026-09-22

### 🛡️ 数据安全与备份增强 (Data Security & Backup)
- **版本快照历史 (Snapshots)**：新增每个思维导图滚动维护最多 20 个历史快照记录，记录节点数与时间戳，支持随时一键无损回滚。
- **工作区全量备份/还原 (Full Workspace Backup)**：一键将所有导图文件、收集箱碎片及版本快照打包导出为 `.mindflow-workspace-backup.json`，并支持标准化全量导入还原。
- **存储配额监控 (Storage Quota)**：动态计算 `chrome.storage.local` 已用空间与上限容量，提供可视化进度条，防止极端情况下本地写入溢出。

### 🎨 10 款专业主题扩充 (10 Theme Systems)
- **经典商务蓝 (`classic-blue`)**：清爽高效的现代办公风格。
- **极夜星云深色 (`dark-nebula`)**：OLED 护眼深黑，高对比度荧光点缀。
- **马卡龙糖果粉 (`macaron`)**：柔和柔美多色调，适合创意灵感整理。
- **森林秘境护眼绿 (`forest-green`)**：低饱和度柔和护眼绿意。
- **极简水墨黑白 (`minimal-ink`)**：高对比度纯粹水墨风，专注内容排版。
- **暖阳琥珀 (`warm-amber`)**：温暖温馨的橙黄基调，适合读书与日常随笔。
- **赛博朋克霓虹 (`cyberpunk-neon`)**：紫黑底色配高亮荧光青与粉红。
- **北欧冰霜蓝 (`nordic-frost`)**：冷冽清透的北欧极简极光质感。
- **学术纸感 (`academic-paper`)**：复古米黄纸质感，适合论文框架与文献研读。
- **薰衣草梦境 (`lavender-dream`)**：典雅紫罗兰质感，适合女性与艺术设计。

### 🧭 左右端人机工程学模块重构 (Ergonomic Layout)
- **左侧宏观工作台 (`LeftWorkbench`)**：
  - 采用 44px 极简图标导航轨 + 280px 平滑展开抽屉，不挤占画布空间。
  - 深度整合：① 文档库管理（创建/搜索/删除）；② 结构大纲（双击直接在树上行内编辑文本）；③ 灵感收集箱（划词入库与一键入图）；④ 数据安全与快照面板。
- **右侧微观检视器 (`PropertySidebar`)**：
  - 作为选定节点的属性面板，仅在节点激活时展示样式、任务属性、标签、超链与备注。
- **停靠位置自由对调 (`dockPosition`)**：
  - 支持一键将宏观工作台停靠在屏幕左端或右端（自动保存至 `mindflow_dock_pos`），特别适配 Chrome 侧边栏（Side Panel）模式。

---

## [1.1.0] - 2026-09-22
### Added
- **规范文档体系**：建立 `docs/PRODUCT.md`、`ARCHITECTURE.md`、`DATA_MODEL.md`、`TASKS.md`、`DECISIONS.md`。
- **Task 任务化管理**：节点支持一键转为 Task（Todo/Doing/Done），支持原生复选框流转与完成划线特效。
- **浏览器专属 Inbox 收集箱**：划词与网页一键入箱，支持收集箱条目一键生成导图节点。
- **Command Palette 命令面板**：支持 `Ctrl/Cmd + K` 呼出，支持全文快速定位节点与执行操作。
- **模板库系统**：内置头脑风暴、SWOT 分析、读书笔记、敏捷规划、文献综述等多套专业模板。
- **Zen 专注模式**：一键隐藏所有界面干扰，提供极致全屏沉浸创作体验。

---

## [1.0.0] - 2026-09-22
### Added
- 基础 Manifest V3 扩展骨架，三级入口（Popup、Side Panel、Full Tab）。
- 左右平衡思维导图、向右单向逻辑图、向下组织架构图三大布局。
- 全键盘盲操快捷键（Tab/Enter/Delete/方向键）与 Undo/Redo 历史栈。
- PNG/SVG/Markdown/JSON 导入导出与双向大纲视图。
